projection;
strict ( 2 );

define behavior for ZC_dmo_travel //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _BOOKING { create; }
}

define behavior for ZC_dmo_bookings //alias <alias_name>
{
  use update;
  use delete;

  use association _TRAVEL;
  use association _BOOKSUPPL { create; }
}

define behavior for zc_dmo_book_suppl //alias <alias_name>
{
  use update;
  use delete;

  use association _travel;
  use association _booking;
}